# Leave empty when the UI is served from the same origin as the API (production build in Express).
# For local Vite dev, leave empty — requests are proxied to the backend.
# For a separate static host (e.g. Netlify) pointing at a remote API, set the full API origin:
# VITE_API_URL=https://your-api.onrender.com

VITE_API_URL=https://finance-data-processing-backend-5yzw.onrender.com
